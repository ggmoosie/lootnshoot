# CODEMAP — LootNShoot

Auto-generated by `scripts/gen-codemap.mjs` — do not edit by hand. Re-run
`node scripts/gen-codemap.mjs` after moving or renaming any exported symbol.

Index of every top-level exported symbol in `js/*.js`, grouped by file.

## js/account.js

- `Account` (const) — js/account.js:120

## js/allies.js

- `Allies` (const) — js/allies.js:13

## js/audio.js

- `Audio` (const) — js/audio.js:4 — audio.js — SYS: Audio. Thin WebAudio blip layer. Event-driven so real SFX can drop in later. No-ops safely where WebAudio is unavailable.

## js/crafting.js

- `Crafting` (const) — js/crafting.js:8

## js/data.js

- `DATA` (const) — js/data.js:3 — data.js — DATA.* pure tables. Tune the game here. No behavior.

## js/enemies.js

- `Enemies` (const) — js/enemies.js:23

## js/fx.js

- `FX` (const) — js/fx.js:10
- `fxTracer` (function) — js/fx.js:8 — one-shot tracer line a->b, auto-removed. Shared by Weapons (player) + Enemies.

## js/gfx.js

- `GFX` (const) — js/gfx.js:7

## js/harvest.js

- `Harvest` (const) — js/harvest.js:12

## js/input.js

- `Input` (const) — js/input.js:16

## js/inventory.js

- `defaultInst` (function) — js/inventory.js:64
- `desItem` (function) — js/inventory.js:80
- `Grid` (class) — js/inventory.js:9
- `Inventory` (const) — js/inventory.js:88
- `newItem` (function) — js/inventory.js:69
- `serItem` (function) — js/inventory.js:73

## js/loot.js

- `Loot` (const) — js/loot.js:13

## js/main.js

_no top-level exports_

## js/mannequin.js

- `buildMannequin` (function) — js/mannequin.js:33 — buildMannequin(equip) -> THREE.Group, centered near origin, facing +Z. equip is S.profile.equip (slots: helmet, armor, rig, backpack, primary, secondary). Any/all slots may be empty — the base body always renders so an unarmored, unarmed character still shows.

## js/minimap.js

- `Minimap` (const) — js/minimap.js:18

## js/objectives.js

- `Objectives` (const) — js/objectives.js:15

## js/perception.js

- `Perception` (const) — js/perception.js:7

## js/player.js

- `Player` (const) — js/player.js:18

## js/preview.js

- `createPreview` (function) — js/preview.js:18 — createPreview(canvas, opts) -> handle. opts: bg : scene.background color (default null = transparent) autoRotate : start auto-rotating (default true) rotateSpeed : radians/sec yaw while auto-rotating (default 0.6) fov : camera field of view (default 42) fitOffset : multiplier on the framing distance (default 1.35; >1 = more margin)

## js/progression.js

- `Progression` (const) — js/progression.js:9

## js/projectiles.js

- `Projectiles` (const) — js/projectiles.js:26

## js/raid.js

- `Raid` (const) — js/raid.js:15

## js/save.js

- `Save` (const) — js/save.js:9

## js/state.js

- `Clock` (const) — js/state.js:37 — global frame clock — Clock.now is wall-time seconds, advanced by the main loop.
- `EQUIP_SLOTS` (const) — js/state.js:34 — equipment slots (order = doll layout + serialization order) (clothing = soft wearable layer; part of the armor/clothing gear system)
- `Events` (const) — js/state.js:9
- `MODE` (const) — js/state.js:23
- `S` (const) — js/state.js:24
- `uid` (const) — js/state.js:41

## js/status.js

- `Status` (const) — js/status.js:13

## js/three.js

- `T` (const) — js/three.js:5
- `THREE` (const) — js/three.js:4 — three.js shim — surface the global CDN build (window.THREE) to ES modules. three stays the classic r128 build loaded in index.html; we do NOT migrate it to ESM. Modules import { T } from "./three.js" (the original code's alias) or { THREE }.

## js/transit.js

- `Transit` (const) — js/transit.js:6

## js/ui.js

- `UI` (const) — js/ui.js:28

## js/util.js

- `clamp` (function) — js/util.js:6 — clamp a value into [a,b]
- `iconFor` (function) — js/util.js:18 — best emoji icon for an item def (per-id override, else per-type, else dot)
- `keyName` (function) — js/util.js:9 — pretty-print a KeyboardEvent.code for the HUD/settings (KeyW -> W, etc.)
- `mulberry` (function) — js/util.js:12 — mulberry32 seeded PRNG factory — deterministic raid geometry per stop
- `rarityColor` (function) — js/util.js:15 — rarity index -> swatch color (used by loot meshes / pickups)

## js/vendor.js

- `Vendor` (const) — js/vendor.js:19

## js/weapons.js

- `Weapons` (const) — js/weapons.js:21

## js/world.js

- `World` (const) — js/world.js:23

